## 🧪 Ejecución de pruebas UI (Selenium + Pytest)

* Las pruebas automatizadas UI se ejecutan localmente usando Selenium y Pytest.
* Usar frameworks (selenium, pytest, webdriver-manager) dentro de entorno virtual para ser más transportable y ordenado el proyecto de prueba


### Ejecutar prueba de login SauceDemo
```bash
 (venv) PS C:\ruta_proyecto\ui-tests\tests>pytest --junitxml=reportLogin.xml  /**reportLogin puede ser cualquier nombre**/
```

